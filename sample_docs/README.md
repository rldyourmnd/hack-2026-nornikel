# Sample Documents

Synthetic fixtures live in `sample_docs/synthetic/`. They are safe to commit and are designed to cover:

- table-backed material/regime/property evidence;
- an explicit method mismatch conflict;
- a missing conductivity gap;
- prompt-injection-like source text that must be treated as untrusted evidence.

Generated binary fixtures are produced by `make generate-fixtures`.
